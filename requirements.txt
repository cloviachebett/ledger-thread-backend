# Trimmed to only what this project actually imports (main.py, app/, alembic/).
# The previous requirements.txt was a full `pip freeze` of a local dev
# environment (400+ packages, including scipy/scikit-learn which this app
# never imports) rather than this project's real dependencies. scipy needs
# a Fortran compiler to build from source, which isn't available on Render's
# build image, so every deploy failed at "Preparing metadata (pyproject.toml)".
fastapi==0.141.1
uvicorn==0.52.0
SQLAlchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12
pydantic==2.13.4
python-multipart==0.0.32
starlette==1.3.1
